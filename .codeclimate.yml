engines:
    radon:
        enabled: true
    pep8:
        enabled: true
    duplication:
        enabled: true
        config:
            languages:
                - python

ratings:
    paths:
        -"**.py"

exclude_paths:
 - "tests/**/*"
 - "scripts/**/*"
 - "unit_tests/**/*"
